//import { useSelector } from "react-redux";

export default function Home() {
  //const { userInfo } = useSelector((state) => state.user);

  return (
    <>
      <div>Home/Messenger</div>
    </>
  );
}
